# 3. Контекст и границы системы (Context and Scope)

> arc42 §3 — что внутри, что снаружи; C4 System Context view из `workspace.dsl`.

Здесь пока нечего сказать — содержимое появится в фазе 6a (HLE-506).
C4-диаграмма уровня Context — в [`workspace.dsl`](../../workspace.dsl), view `systemContext`. Просмотр: `docker run -it --rm -p 8080:8080 -v .:/usr/local/structurizr structurizr/lite`.
