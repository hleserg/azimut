# 6. Поведение в рантайме (Runtime View)

> arc42 §6 — ключевые сценарии взаимодействия компонентов (Mermaid sequenceDiagram).

Здесь пока нечего сказать — содержимое появится в фазе 6b (HLE-507).
Сценарии: индексация, запрос, обновление, фолбэк (ADR 0007), судья (ADR 0003), роутинг graph→metadata→grep (ADR 0026).
Mermaid выбран вместо DSL Dynamic-views для §6 — лучше читается в git diff (ADR 0034).
